# Workflow Context

## Initial Request
Generate file guide for ./app.configs, document UI configuration (aerospace/borders/sketchybar), then compress both files.

## Workflow Purpose
Create comprehensive documentation for UI configuration files to enable future editing and feature additions.

## Method
1. Generate file guide using skll-file-guide skill
2. Use opus agent to analyze and document UI relationships
3. Compress generated files using skll-prompt-compression skill

## Acceptance Criteria
- File guide generated in ./app.configs/
- UI documentation in ./app.configs/guide.ui.md
- Both files compressed for token efficiency
