#!/bin/bash

# Register events (kept for potential future use)
sketchybar --add event window_focus \
           --add event windows_on_spaces \
           --add event title_change

# Center app name display DISABLED
# The title item has been removed per user request
