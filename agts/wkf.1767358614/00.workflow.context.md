# Workflow: JankyBorders Installation

## Objective
Install JankyBorders (window border utility for macOS) and configure it with default settings.

## Request Summary
- Install JankyBorders via Homebrew (FelixKratz/formulae tap)
- Create default configuration in `/Users/teazyou/workspace/app.configs/borders/`
- Set up symlink from `~/.config/borders/` to workspace config
- Start the service via brew services

## Research Findings
- **Tool**: JankyBorders - lightweight window border utility for macOS 14.0+
- **Installation**: `brew tap FelixKratz/formulae && brew install borders`
- **Config Location**: `~/.config/borders/bordersrc`
- **Service**: `brew services start borders`
- **Config Format**: Shell script that invokes borders with options

## Acceptance Criteria
1. JankyBorders installed via Homebrew
2. Config file created at `/Users/teazyou/workspace/app.configs/borders/bordersrc`
3. Symlink: `~/.config/borders` -> `/Users/teazyou/workspace/app.configs/borders`
4. Service running via brew services
5. Report provided with management commands

## Status
- [x] Research completed
- [x] Installation
- [x] Configuration
- [x] Symlink setup
- [x] Service start
- [x] Final report

---

## Execution Report

### Tasks Accomplished

1. **Research** - Fetched JankyBorders README from GitHub, extracted installation and configuration details
2. **Installation** - Installed borders v1.8.4 via Homebrew from FelixKratz/formulae tap
3. **Configuration** - Created default bordersrc with round style, 5.0 width, light/dark border colors
4. **Symlink** - Created symlink `~/.config/borders` -> `/Users/teazyou/workspace/app.configs/borders`
5. **Service** - Started borders service via brew services

### Files Created

| File | Purpose |
|------|---------|
| `/Users/teazyou/workspace/app.configs/borders/bordersrc` | Main configuration file |
| `~/.config/borders` (symlink) | Points to workspace config |

### Service Management Commands

```bash
# Start service
brew services start borders

# Stop service
brew services stop borders

# Restart service
brew services restart borders

# Check status
brew services list | grep borders

# Update configuration at runtime (no restart needed)
borders active_color=0xffe1e3e4 inactive_color=0xff494d64 width=5.0
```

### Configuration Details

- **Style**: round (rounded corners on borders)
- **Width**: 5.0 pixels
- **Active color**: 0xffe1e3e4 (light gray)
- **Inactive color**: 0xff494d64 (dark gray)
- **HiDPI**: off
