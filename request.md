# Request

## Read Aerospace doc

`./app.configs/features.aerospace.md`
`./app.configs/doc.aerospace.md`

## Obtain path of AeroSpace config files

Aerospace: `./app.configs/aerospace`
sketchybar: `./app.configs/sketchybar`
JankyBorder: `./app.configs/borders`

## Add Launch app Shortcut

[APPLE] + 1 = Open (or focus on) Brave, when open, by default on workspace 1
[APPLE] + 2 = Open (or focus on) iTerm, when open, by default on workspace 2
[APPLE] + 3 = Open (or focus on) VSCode, when open, by default on workspace 3
[APPLE] + 4 = Open (or focus on) Obsidian, when open, by default on workspace 4
[APPLE] + 5 = Open (or focus on) Claude Desktop, when open, by default on workspace 5
[APPLE] + ` = Open (or focus on) Finder, when open, by default on workspace 5

## Issue top padding

Why on my main computer, `outer.top = 40` nicely allign with the top bar of SketchyBar, however on second monitor it looks like there is much more than 40, more like 100 padding on the second monitor.

Can you fix it ?